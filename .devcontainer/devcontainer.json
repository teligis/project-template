{
    "name": "claude-code-env",
    "build": {
        "dockerfile": "Dockerfile"
    },
    "postCreateCommand": "sudo chown -R vscode:vscode /workspaces && if [ ! -f .env ]; then cp .env.example .env && echo '✅ .env已生成，请填入DeepSeek API Key后重启终端'; fi",
    "customizations": {
        "vscode": {
            "extensions": [
                "anthropic.claude-code"
            ]
        }
    }
}
